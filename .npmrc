# Pin the public npm registry so dependency resolution stays consistent across
# machines (avoids accidental use of a private mirror) and documents intent if we
# migrate registries later.
registry=https://registry.npmjs.org/
